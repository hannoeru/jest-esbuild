{
  "name": "fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "DEBUG=jest-esbuild* jest"
  },
  "devDependencies": {
    "@jest/types": "^28.1.1",
    "jest": "^28.1.2",
    "jest-esbuild": "workspace:*",
    "typescript": "^4.7.4"
  }
}
